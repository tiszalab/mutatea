# flu_cli
This is a framework comparing the mutational spectra of IAV between sources/cohorts.

The point is ****

`Inputs`: metadata, paired-end short reads, reference fna and gff

`Outputs`: 

# Installation

1. Clone this github repository

2. Create the `conda` environment using the .yaml file

3. activate the environment

4. Use pip to install this command line tool
   `cd flu_cli`
   `pip install .`
