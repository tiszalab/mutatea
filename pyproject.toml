[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mutatea"
version = "0.1.0"
authors = [
  { name="Camille R. Mazurek", email="camillemazurek23@gmail.com" },
]
description = "A framework comparing the mutational spectra of virome sequencing data between sources & cohorts."
readme = "README.md"
requires-python = ">=3.13"

classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "pandas",
    "biopython",
    "openpyxl",
    "polars",
    "pysam",
    "varmint @ https://github.com/tiszalab/varmint/archive/refs/tags/v0.2.0.tar.gz"
    ]

[tool.setuptools]
py-modules = ["mutatea", "mutatea_funcs"]

[project.scripts]  
mutatea = "mutatea:mutatea"

[project.urls]
"Homepage" = "https://github.com/tiszalab/mutatea"